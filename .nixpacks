# Nixpacks configuration for Coolify
# This file tells Coolify to use Node.js instead of auto-detecting Deno

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l 3000"
